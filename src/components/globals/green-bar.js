import React, { Component } from 'react'

// Styles
import styles from './styles/green-bar.module.sass'

const GreenBar = ({props}) => (
  <section className={styles.bar} />
)

export default GreenBar
